db:
  image: postgres:9.4

redis:
  image: redis:latest

web:
  build: .
  command: warehouse --config dev/config.yml serve --reload --bind 0.0.0.0
  ports:
    - "80:8000"
  volumes:
    - .:/app/
  links:
    - db
    - redis
