db:
  image: postgres:9.4

redis:
  image: redis:latest

web:
  build: .
  command: warehouse --config dev/config.yml serve -b 0.0.0.0 -w 1 --reload
  ports:
    - "80:8000"
  volumes:
    - .:/app/
  links:
    - db
    - redis
  environment:
    PYTHONUNBUFFERED: 1
