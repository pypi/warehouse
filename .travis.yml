language: python
python:
  - "3.3"
install:
  - pip install -r requirements.txt
script:
  - invoke tests --suite $SUITE
branches:
  only:
    - master
env:
  - SUITE=unit
  - SUITE=functional
  - SUITE=coverage
