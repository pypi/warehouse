site:
    access_token: testing

assets:
    directory: "static"

database:
    url: "postgresql://localhost/test_warehouse"

redis:
    downloads: "redis://localhost:6379/0"
    sessions: "redis://localhost:6379/0"

search:
    hosts:
        - host: 127.0.0.1
          port: 9200
